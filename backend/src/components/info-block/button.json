{
  "collectionName": "components_info_block_buttons",
  "info": {
    "displayName": "button",
    "description": ""
  },
  "options": {},
  "attributes": {
    "text": {
      "type": "string",
      "required": true
    },
    "color": {
      "type": "enumeration",
      "enum": [
        "turquoise",
        "orange",
        "beige"
      ],
      "default": "turquoise",
      "required": true
    },
    "slug": {
      "type": "string",
      "regex": "[a-z0-9]+(?:-[a-z0-9]+)*$",
      "required": true
    }
  }
}
